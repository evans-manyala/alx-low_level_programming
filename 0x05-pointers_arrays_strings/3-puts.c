#include "main.h"
#include <stdio.h>
/**
 * _puts - function prints a string followed by a newline.
 * @str: string value being returned followed by a new line.
 * Return: void
 */
void _puts(char *str)
{
	printf("%s\n", str);
}
