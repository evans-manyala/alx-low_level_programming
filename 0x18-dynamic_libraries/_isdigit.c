#include "main.h"
/**
 * _isdigit - Check if the character is a digit using ASCII values
 * @c: Character value.
 * Return: Return a value if its a digit
 */
int _isdigit(int c)
{
	return ((c >= '0' && c <= '9'));
}
