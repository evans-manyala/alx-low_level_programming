#include "main.h"
/**
 * _islower - Checks if the character is lowercase using ASCII values.
 * @c: Character to be checked.
 * Return: Character in lowercase.
 */
int _islower(int c)
{
	return ((c >= 'a' && c <= 'z'));
}
